_schema-version: "3.2"
ID: Prod2
version: 0.0.1
modules:
- name: prod2-approuter
  type: approuter.nodejs
  path: prod2-approuter
  requires:
  - name: Prod2_html_repo_runtime
  - name: dest_Prod2
  parameters:
    disk-quota: 256M
    memory: 256M
- name: Prod2_ui_deployer
  type: com.sap.application.content
  path: .
  requires:
  - name: Prod2_html_repo_host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - Prod2-content.zip
      name: Prod2
      target-path: resources/
- name: Prod2
  type: html5
  path: Prod2
  build-parameters:
    builder: custom
    commands:
    - npm run build
    supported-platforms: []
resources:
- name: Prod2_html_repo_runtime
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-runtime
- name: Prod2_html_repo_host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-host
- name: dest_Prod2
  type: org.cloudfoundry.managed-service
  parameters:
    service: destination
    service-plan: lite
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm install
